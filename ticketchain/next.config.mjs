/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placekitten.com', 'placepuppy.net', 'placebear.com', 'placeimg.com', 'loremflickr.com'],
      },
};

export default nextConfig;
